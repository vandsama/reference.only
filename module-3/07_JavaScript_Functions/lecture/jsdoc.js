
/**
 * 
 * @param {number} a     -- the first adder
 * @param {number} b     -- the second adder
 * @returns {number}     -- the sum
 */
function add(a,b) {
    return a + b;
}


const sum = add(1,1);

[].red